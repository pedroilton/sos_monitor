
<!-- Formulário de users/new -->
<div>
    <div class="container mt-3">

      <div class="system-area">
    <!-- Início do Formulário de cadastro -->
        <%= image_tag 'cad_turmas', class:"d-none d-sm-block" %>
        <div class="system-area-data">
              <h2>Cadastro de Turmas</h2>
            <%= simple_form_for(@classes) do |f| %>
              <!-- form-inputs -->
              <div class="form-inputs">
              <div>
                <%= f.association :discipline,
                              label: 'Disciplina',
                              placeholder: 'Disciplina',
                              required: true,
                              autofocus: true %>
              </div>
              <div>
                <%= f.input :title,
                              label: 'Turma',
                              placeholder: 'Turma',
                              required: true,
                              autofocus: true %>
              </div>
              <div>
                  <%= f.association :professor_id,
                              label: 'Professor',
                              placeholder: 'Professor',
                              required: true,
                              autofocus: true %>
              </div>
             
            </div>

            <!-- form-inputs -->
            <!-- form-actions -->
            <div class="form-actions">
              <%= f.button :submit, "Adicionar", class:"btn btn-primary mb-2" %>
            </div>
            <div class="form-actions">
              <%= link_to "Vincular Aluno", new_session_path(resource_name), class:"btn btn-outline-primary" %>
            </div>
            <!-- f -->
          </div>
  

        </div>
        <!-- container -->
      <% end %>

      
      </div>
  <!-- Fim do Formulário de cadastro -->

    </div>
</div>
</div>